/**
 * Ethereum mainnet contract addresses shared by decoder tests / fixtures (not calldata bodies).
 */

/** USDT0 OFT (LayerZero) — `to` for `USDT0_LZ_SEND_MAINNET` in `decoderCalldataHex.ts` tests. */
export const USDT0_OFT_MAINNET = '0x6c96de32cea08842dcc4058c14d3aaad7fa41dee' as const;
